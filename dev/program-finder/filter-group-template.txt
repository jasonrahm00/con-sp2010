<label for="{{ groupString + index }}">
  <input id="{{ groupString + index }}" type="checkbox"
         data-ng-model="inputValue">
  <span class="label-text">{{ label }}</span>
  <span class="result-count">({{ count }})</span>
</label>
